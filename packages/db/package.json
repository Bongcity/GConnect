{
  "name": "@gconnect/db",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate && prisma generate --schema=prisma/schema-ddro.prisma",
    "generate:main": "prisma generate",
    "generate:ddro": "prisma generate --schema=prisma/schema-ddro.prisma",
    "push": "prisma db push",
    "studio": "prisma studio",
    "studio:ddro": "prisma studio --schema=prisma/schema-ddro.prisma",
    "migrate": "prisma migrate dev",
    "format": "prisma format",
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.9.1",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
